import { Encriptador } from "../../src/config";

jest.mock('../src/presentation/server');

describe('Test Encriptador.ts', () => {
    
    test('Crear el hash', async() => {
        Encriptador
    });
    
});